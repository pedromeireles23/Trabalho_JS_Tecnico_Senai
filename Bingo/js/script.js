function tab(){
    //gerar numeros 1 até 99
    let numeros =[0]
    for(let count=1;count<100;count++){
        numeros ==[count]
        document.getElementById.innerHTML('tabela').innerHTML= +numeros;
        console.log(numeros.length)
        
    }
}